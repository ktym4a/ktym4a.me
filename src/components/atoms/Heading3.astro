---
import type { ColorType } from '@/types/index'

interface Props {
    baseColor: ColorType
    id: string
}

const { baseColor, id } = Astro.props
---

<h3
    class={`flex break-words text-lg font-bold before:content-['###'] before:text-ctp-text before:pr-3 md:text-xl ${baseColor.text}`}
    id={id}><slot /></h3
>
